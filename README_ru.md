# 🔐 CryptoWallet - Безопасный Микроконтроллерный Кошелек Bitcoin

**Производственный-готовый аппаратный кошелек, демонстрирующий архитектуру современных встроенных систем**

Безопасное подписание Bitcoin-транзакций на **STM32H743** с продвинутым управлением памятью, планированием задач реального времени, многопротокольной связью и криптографическим подписанием.

**Языки:** 🇬🇧 [English](README.md) | 🇷🇺 [Русский](README_ru.md) | 🇵🇱 [Polski](README_pl.md)

---

## ⚡ Основные Характеристики

| Аспект | Детали |
|--------|--------|
| **MCU** | STM32H743ZI (Cortex-M7 @ 480 МГц) |
| **Память** | 2 МБ Flash + 1 МБ SRAM |
| **ОС** | FreeRTOS (многозадачность реального времени) |
| **Сеть** | LwIP (Ethernet + HTTP сервер) |
| **Крипто** | trezor-crypto (secp256k1, BIP-39/32) |
| **Интерфейсы** | HTTP, WebUSB, UART, I2C (OLED) |
| **Безопасность** | ECDSA подпись, производная ключей, memzero() очистка буферов |

---

## 🏗️ Архитектура Системы

### Карта Памяти и Организация

```
FLASH ПАМЯТЬ (0x08000000 - 0x08200000):
├─ 0x08000000: [Загрузчик (опционально) - 64 КБ]
├─ 0x08010000: [Приложение - 1.5 МБ]
│   ├─ Ядро FreeRTOS
│   ├─ Стек LwIP
│   ├─ Библиотека trezor-crypto
│   ├─ Прикладное ПО
│   └─ Конфигурация
└─ 0x08180000: [Свободное место - 512 КБ]

SRAM ПАМЯТЬ (0x20000000 - 0x20100000):
├─ 0x20000000: [FreeRTOS TCB и объекты ядра - ~100 КБ]
│   ├─ Task Control Blocks
│   ├─ Ready lists
│   └─ Структуры очередей/событий
├─ 0x20018000: [IPC объекты - ~50 КБ]
│   ├─ tx_request_queue
│   ├─ sign_response_queue
│   ├─ display_queue
│   └─ Mutex/семафоры
├─ 0x20025800: [Стеки задач - ~300 КБ]
│   ├─ Стек задачи подписания (32 КБ)
│   ├─ Стек сетевой задачи
│   ├─ Стек задачи дисплея
│   ├─ Стек задачи ввода
│   └─ Стек IO задачи
├─ 0x20080000: [Динамическая куча - ~400 КБ]
│   ├─ Буферы LwIP
│   ├─ malloc/free (crypto)
│   └─ USB буферы
└─ 0x200F8000: [RX дескрипторы LwIP - 64 КБ]
```

**Ключевой принцип дизайна:** Раздельные регионы памяти для ядра, задач и кучи для предотвращения фрагментации.

### Детали управления памятью

**Стратегия организации FLASH:**
- **Изоляция загрузчика**: Опциональный пользовательский загрузчик на фиксированной позиции (граница 64 KB)
- **Образ приложения**: Привязан к начиналу 0x08010000 (смещение 64 KB)
- **Преимущество**: Если загрузчик падает, ROM загрузчик может загрузить тестовое приложение
- **Конфигурация**: Отредактируйте `linker.ld` для изменения базовых адресов

**Стратегия организации SRAM:**
- **Объекты ядра первыми** (TCB, ready lists): Предотвращает переполнение структур ядра
- **Слой IPC** (очереди, mutex): Отдельный регион предотвращает фрагментацию
- **Стеки задач** (отдельно для каждой): Размеры стеков предвыделены (не растут)
  - **Задача подписания**: 32 KB (криптография нуждается в пространстве буфера)
  - **Сетевая задача**: 16 KB (требования стека LwIP)
  - **Задача дисплея**: 4 KB (минимальная работа за tick)
  - **Другие задачи**: 2-8 KB (управляемые событиями)
- **Динамическая куча** (400 KB): Используется malloc/free (библиотека крипто, буферы LwIP)
- **Обоснование дизайна**:
  - Размеры стеков ФИКСИРОВАНЫ - исключает уязвимость динамического выделения
  - Куча ограничена - предотвращает неконтролируемое выделение
  - Если куча исчерпана: OOM ошибка вместо молчаливой коррупции

**Защита памяти (ARM Cortex-M7):**
- **MPU (Memory Protection Unit)**: Опциональная конфигурация
  - Может защитить код ядра от коррупции задачами
  - Может защитить чувствительные регионы памяти (ключи в RAM)
  - Не включено в настоящий момент (добавляет задержку, требует профилирования)

---

## 🔄 Архитектура Kernel & System Software

### Организация Kernel FreeRTOS

**Системный тик и планирование:**
- **Исключение SysTick**: Настроено срабатывать каждые 1 мс
- **Поток обработчика тика:**
  1. Сохранить состояние CPU (регистры сохранены железом Cortex-M7)
  2. Вызвать `xTaskIncrementTick()` → обновить глобальный счётчик тиков
  3. Оценить готовность всех задач с истекшими задержками
  4. Найти готовую задачу с наивысшим приоритетом
  5. Если отличается от текущей → пометить исключение PendSV
  6. Вернуться из прерывания
  7. Исключение PendSV → переключение контекста (сохранить старый, загрузить новый)
- **Время переключения контекста**: ~5-10 микросекунд (аппаратно ускоренно)
- **Контекст включает**: 16 регистров (R0-R15), XPSR (флаги), регистр управления

**Очередь готовности задач:**
- **Структура данных**: 32 связанных списка (один на уровень приоритета 0-31)
- **O(1) поиск**: Готовая задача с наивысшим приоритетом известна немедленно
- **Lock-free** (в контексте ISR): Использует атомные операции с битовыми полями
- **Пример**: Если приоритеты 22, 21, 20 имеют готовые задачи, планировщик выбирает 22

**Механизм задержки задач:**
- **Список таймеров**: Отсортирован по времени пробуждения
- **Обработка переполнения**: Использует 16-битное оборачивание таймера (FreeRTOS v10+)
- **Поток пробуждения**: Список таймеров → очередь готовности → следующая оценка SysTick

**Наследование приоритета (Mutex):**
- **Проблема**: Инверсия приоритета (низкоприоритетная задача, держащая mutex, блокирует высокоприоритетную)
- **Решение**: Когда задача ждёт на mutex, временно поднимает приоритет держателя замка
- **Пример в коде**: `crypto_lock` mutex использует наследование приоритета
  - Если task_net ждёт crypto_lock, task_sign получает повышенный приоритет
  - После того как task_sign освобождает замок, приоритет падает обратно

### Паттерны синхронизации задач

**Коммуникация на основе очереди (tx_request_queue → task_sign):**
```c
// Производитель (task_net):
xQueueSend(tx_request_queue, &tx_request, portMAX_DELAY);
// Блокируется если очередь полна (обычно нет, глубина=10)

// Потребитель (task_sign):
xQueueReceive(tx_request_queue, &tx_request, portMAX_DELAY);
// Блокируется до получения сообщения
// Атомарно удаляет сообщение из очереди
// Пробуждается даже если другие задачи выше приоритета (есть данные)
```

**Группа событий (user_confirm_event):**
```c
// Ожидающий (task_sign):
xEventGroupWaitBits(user_confirm_event, CONFIRM_BIT, 
                    pdTRUE,      // очистить при выходе
                    pdTRUE,      // ждать всех бит
                    xTicksToWait); // timeout: 30 секунд

// Сигнализирующий (task_user):
xEventGroupSetBitsFromISR(user_confirm_event, CONFIRM_BIT, &xHigherPriorityTaskWoken);
```

**Mutex с таймаутом (crypto_lock):**
```c
// Задача берёт замок с таймаутом 5 секунд:
if (xSemaphoreTake(crypto_lock, pdMS_TO_TICKS(5000)) == pdTRUE) {
    // Выполнить крипто-операцию
    xSemaphoreGive(crypto_lock);
} else {
    // Таймаут - другая задача держит замок слишком долго
    // Действие: ошибка ответа или повтор
}
```

### Анализ ограничений реального времени

**Hard Real-Time (не должно пропускаться):**
- **SysTick** → дедлайн 1 мс (аппаратный)
- **Задержка прерывания** → макс ~10 µs (ограничение ARM ядра)
- **Критический раздел**: Отключенные прерывания ≤ 100 µs (предотвращает долгий jitter)

**Soft Real-Time (пропущенные дедлайны деградируют, не критичны):**
- **Ответ на нажатие кнопки**: ≤ 100 мс приемлемо (debounce + интервал опроса)
- **Дисплей OLED**: ≤ 100 мс приемлемо (восприятие человеческим глазом)
- **HTTP ответ**: ≤ 1 секунда приемлемо (пользовательское взаимодействие)

**Анализ блокирования задач:**
- **task_sign блокирована в WAIT_USER**: Наивысшей приоритета задача блокирована
  - Другие задачи работают: task_net, task_display, task_io, task_user
  - Сетевой трафик всё ещё обслуживается (task_net)
  - Нет голодания (все задачи получают время CPU)
- **Планирование по приоритетам гарантирует**:
  - Нажатие кнопки (task_user, приоритет 22) всегда вытесняет других
  - Подписание (приоритет 21) вытесняет сеть (приоритет 20)
  - Сеть не голодает более низких приоритетов

---

## 📋 Архитектура Задач (FreeRTOS)

### Планируемые Задачи

```
Приоритет 22: Задача пользовательского ввода (task_user.c)
├─ Высший приоритет (взаимодействие пользователя)
├─ Опрос кнопки (PC13, интервалы 20мс)
├─ Дебаунс (30мс стабильного обнаружения)
└─ Сигналы: user_confirm_event → Задача подписания

Приоритет 21: Задача подписания (task_sign.c)
├─ FSM-ориентированный конвейер подписей
├─ Ожидает: tx_request_queue
├─ Вызывает: crypto_wallet.c → trezor-crypto
├─ Отправляет: sign_response_queue
└─ Продолжительность: ~100-500ms (за подпись)

Приоритет 20: Сетевая задача (task_net.c)
├─ HTTP сервер (LwIP)
├─ Слушает: порт 80, WebUSB bulk endpoints
├─ Ставит в очередь: tx_request_queue
└─ Ответ: HTTP 200 + подпись

Приоритет 18: IO задача (task_io.c)
├─ Индикаторы состояния LED
├─ Паттерны: постоянно, мигание, ошибка
└─ Визуальная обратная связь пользователю

Приоритет 15: Задача дисплея (task_display.c)
├─ Управление SSD1306 OLED (128×32)
├─ Обновления: 10 Гц (каждые 100ms)
├─ Состояние: "Ожидание...", "Подписание...", "✓ OK", "✗ Ошибка"
└─ Прокручиваемый журнал: последние 4 транзакции

SysTick Прерывание: 1 ms
├─ Планируется FreeRTOS
├─ Переключение контекста
└─ Проверка готовности задач
```

### IPC (Межпроцессное Взаимодействие)

```
Очередь: tx_request_queue
├─ Источник: task_net (обработчик HTTP)
├─ Назначение: task_sign (FSM подписания)
├─ Сообщение: {сумма, адрес, валюта}
└─ Блокирующая: task_sign ждет здесь

Очередь: sign_response_queue
├─ Источник: task_sign (после подписи)
├─ Назначение: task_net (HTTP ответ)
├─ Сообщение: {подпись (DER), tx_id}
└─ Неблокирующая: отправка и продолжение

Событие: user_confirm_event
├─ Источник: task_user (нажатие кнопки)
├─ Слушатель: task_sign (заблокирована на шаге подписи)
├─ Разблокирует: вычисление подписи
└─ Таймаут: 30 секунд

Mutex: crypto_lock
├─ Защищает: доступ к библиотеке trezor-crypto
├─ Владелец: task_sign во время подписания
├─ Предотвращает: одновременные крипто операции
└─ Наследование приоритета: предотвращает инверсию
```

---

## 🔐 Процесс Загрузки и Цепь Безопасности

### Последовательность Загрузки

```
ШАГ 1: Вектор сброса (STM32H743 ROM)
├─ CPU запускается @ 0x08000000
├─ Загружает Stack Pointer (SP) из вектора
├─ Прыжок в Reset_Handler
└─ Время: < 1ms

ШАГ 2: Загрузчик (опционально - от stm32_secure_boot)
├─ Местоположение: 0x08000000 (64 КБ)
├─ Операции:
│   ├─ Конфигурация тактирования (PLL @ 480 МГц)
│   ├─ Чтение образа приложения (0x08010000)
│   ├─ Вычисление хеша SHA-256
│   ├─ Проверка подписи ECDSA
│   ├─ Проверка подписи с открытым ключом
│   └─ Прыжок в приложение если валидно
├─ Fallback: LED ошибка + остановка если невалидно
└─ Время: ~50-100ms

ШАГ 3: Инициализация приложения (main.c)
├─ Инициализация оборудования (hw_init.c):
│   ├─ Конфигурация тактирования (HSI48 для USB)
│   ├─ Настройка GPIO (UART, I2C, USB AF)
│   ├─ Инициализация UART (USART3 @ 115200)
│   ├─ Инициализация I2C (100 кГц для OLED)
│   ├─ Инициализация USB устройства (FS @ 12 Мбит/с)
│   ├─ Инициализация PHY Ethernet (RMII)
│   ├─ Инициализация RNG (если USE_RNG_DUMP=1)
│   └─ Настройка NVIC (приоритеты прерываний)
│
├─ Создание ядра FreeRTOS:
│   ├─ Инициализация кучи (400 КБ доступно)
│   ├─ Создание очереди таймеров
│   ├─ Настройка структуры планировщика
│   └─ Распределение пулов памяти
│
├─ Создание IPC объектов:
│   ├─ Создать tx_request_queue (глубина: 10)
│   ├─ Создать sign_response_queue (глубина: 5)
│   ├─ Создать display_queue (глубина: 20)
│   ├─ Создать crypto_lock mutex
│   └─ Создать user_confirm_event
│
├─ Создание задач (порядок приоритета):
│   ├─ task_user (Приоритет 22, 8 КБ стека)
│   ├─ task_sign (Приоритет 21, 32 КБ стека)
│   ├─ task_net (Приоритет 20, 16 КБ стека)
│   ├─ task_io (Приоритет 18, 2 КБ стека)
│   └─ task_display (Приоритет 15, 4 КБ стека)
│
└─ Запуск планировщика FreeRTOS:
    ├─ SysTick сконфигурирован (1 мс тик)
    ├─ Idle задача создана
    └─ Переключение контекста на готовую задачу

ШАГ 4: Время выполнения (управляется SysTick)
├─ Каждые 1 мс:
│   ├─ Срабатывает исключение SysTick
│   ├─ FreeRTOS увеличивает счетчик тиков
│   ├─ Оценивается готовность задач
│   ├─ Переключение контекста если нужно
│   └─ Возврат к запущенной задаче
└─ Задачи ожидают событий/очередей по мере необходимости
```

### Цепь Доверия (с загрузчиком)

```
┌─ STM32H743 ROM Загрузчик
│  (неизменяемый с завода, зашитый @ 0x00000000)
│  └─ Загружает и валидирует загрузчик пользователя
│
└─ Загрузчик пользователя (stm32_secure_boot)
   (опционально, расположен @ 0x08000000)
   ├─ Хеш SHA-256 приложения
   ├─ Проверка подписи ECDSA
   ├─ Открытый ключ в keys.h
   └─ Прыжок в app @ 0x08010000 если валидно
      │
      └─ Приложение CryptoWallet
         └─ Выполняется только если проверка загрузчика пройдена
```

---

## 🔄 Поток Подписания Транзакции

### Детальная Машина Состояний (task_sign.c)

```
[IDLE] - Ожидание запроса
  ↓
[QUEUE_WAIT] - Блокирована на tx_request_queue
  ↓ (получает: {сумма, адрес, валюта})
[VALIDATE] - tx_request_validate.c
  ├─ Проверка формата адреса (Base58/Bech32)
  ├─ Валидация суммы (>0, <21M BTC)
  ├─ Проверка валюты (BTC mainnet/testnet)
  └─ ✗ Невалидно → [ERROR] → HTTP 400
    ✓ Валидно → [KEY_DERIVE]
  ↓
[KEY_DERIVE] - crypto_wallet.c
  ├─ Чтение seed (из RAM или test_seed)
  ├─ Генерация мастер-ключа через HMAC-SHA-512
  ├─ Применение пути BIP-32: m/44'/0'/0'/0/0
  ├─ Деривация приватного ключа потомка
  └─ Сохранение в защищенный буфер
  ↓
[DISPLAY] - task_display.c
  ├─ Показать: "Подтвердить? {сумма} BTC на {адр...}"
  ├─ Обновить OLED дисплей
  └─ Состояние → "Ожидание подтверждения"
  ↓
[WAIT_USER] - Блокирована на user_confirm_event
  ├─ Пользователь нажимает кнопку (PC13)
  ├─ task_user дебаунсит (30мс)
  ├─ Сигналит user_confirm_event
  └─ Таймаут: 30 секунд → [TIMEOUT_ERROR]
    ✓ Подтверждено → [SIGN]
  ↓
[SIGN] - crypto_wallet.c → trezor-crypto
  ├─ Сообщение для подписи: байты транзакции
  ├─ Алгоритм хеширования: SHA-256(транзакция)
  ├─ Алгоритм подписи: ECDSA secp256k1
  ├─ Генерация nonce: случайная или RFC 6979
  ├─ Выход подписи: пара (r, s)
  ├─ DER кодирование: (0x30, длина, ...)
  └─ Продолжительность: 50-200ms
  ↓
[MEMZERO] - memzero.c
  ├─ Очистить: буфер приватного ключа
  ├─ Очистить: буфер seed
  ├─ Очистить: промежуточные значения
  ├─ Метод: волатильные записи (предотвращение оптимизации)
  └─ Проверить: буферы = 0x00
  ↓
[RESPONSE] - Ставка результата в очередь
  ├─ Отправить: sign_response_queue
  ├─ Сообщение: {подпись, tx_id}
  ├─ task_net получает и строит HTTP ответ
  └─ HTTP 200 + JSON {"signature": "...", "tx_id": "..."}
  ↓
[SUCCESS] - Обновление дисплея
  ├─ Показать: "✓ Подпись OK"
  ├─ Продолжительность: 2 секунды
  └─ Возврат в [IDLE]
```

---

## 🔌 Обработка Прерываний

### Иерархия Приоритетов Прерываний

```
Приоритет 0 (Высший - Hard Real-time):
├─ USART3_IRQHandler (получение UART)
├─ I2C1_EV_IRQHandler (I2C OLED)
└─ USB прерывания (события устройства)

Приоритет 1-2 (Средний - FreeRTOS-управляемый):
├─ SysTick_Handler (1 мс тик) ⭐
├─ Ethernet прерывания (LwIP)
└─ GPIO прерывания (кнопка)

Приоритет 3-7 (Низший - Отложенный):
└─ Системные исключения (обработчики ошибок)

Детали SysTick Handler:
├─ Срабатывает каждые 1 мс (настраивается)
├─ Запускает: xTaskIncrementTick() (FreeRTOS)
├─ Операции:
│   ├─ Увеличение счетчика тиков
│   ├─ Обновление таймеров задач
│   ├─ Проверка готовности задачи более высокого приоритета
│   ├─ Установка исключения pPendSV если переключение нужно
│   └─ Возврат к текущей задаче ИЛИ
│       Pending SVC → переключение контекста
└─ Общая задержка: ~5-10 микросекунд
```

### UART Прерывание (Debug/Логирование)

```
Когда данные получены на USART3:
├─ Срабатывает USART3_IRQHandler
├─ Читает: один байт из регистра данных RX
├─ Операция: xQueueSendFromISR(uart_queue, ...)
├─ Может пробудить: task_display_minimal (если ждет)
└─ Возврат: FreeRTOS yields если задача пробуждена

Предотвращает: блокировку основного цикла
Включает: отзывчивое логирование
```

---

## 🔐 Авторизация и Аутентификация

### Подтверждение Пользователя (Авторизация)

```
Уровень 1: Нажатие кнопки (GPIO PC13)
├─ Требуется физическое подтверждение
├─ task_user читает состояние пина каждые 20мс
├─ Дебаунс: 30мс стабильного обнаружения
├─ Нажатие → Сигналит user_confirm_event
└─ task_sign: разблокируется и продолжает подписание

Безопасность: 
├─ ✓ Предотвращает случайные транзакции
├─ ✓ Пользователь должен быть физически присутствующим
└─ ✗ Без криптографической аутентификации (по дизайну)

Альтернатива: PIN (Опциональное расширение)
├─ Может добавить ввод числового PIN на OLED
├─ Проверить PIN с сохраненным хешем
├─ Ограниченные попытки (3 удара)
└─ Таймаут после неудачных попыток
```

### Валидация Транзакции (Аутентификация)

```
Слой 1: Проверка формата адреса
├─ Типы адресов Bitcoin:
│   ├─ P2PKH (legacy): "1..." (Base58Check)
│   ├─ P2SH (multisig): "3..." (Base58Check)
│   └─ SegWit (native): "bc1..." (Bech32)
├─ Проверка контрольной суммы: Base58Check или Bech32
└─ Результат: ✓ Валидный формат ИЛИ ✗ Невалидный → Отклонить

Слой 2: Валидация суммы
├─ Диапазон: 0 < сумма ≤ 21,000,000 BTC
├─ Десятичная точность: до 8 мест (1 Satoshi = 0.00000001 BTC)
├─ Проверка: Нет отрицательных сумм
└─ Результат: ✓ Валидно ИЛИ ✗ Вне диапазона → Отклонить

Слой 3: Валидация валюты
├─ Поддерживаемые: BTC mainnet, testnet3
├─ Отклоненные: Альтмонеты (BCH, LTC, и т.д.)
└─ Будущее: Расширяемый whitelist

Криптографическая валидация: Проверка ECDSA
├─ Подпись: пара (r, s) от подписания
├─ Открытый ключ: Выведен из приватного ключа через secp256k1
├─ Сообщение: SHA-256(транзакция)
├─ Проверка: Q = [s^-1 * (R + r * Qa)] (mod p)
├─ Результат: ✓ Валидная подпись ИЛИ ✗ Невалидная
└─ Только валидные подписи используемы в блокчейне
```

---

## 🚀 Сборка и Прошивка

### Флаги конфигурации сборки

```bash
make USE_CRYPTO_SIGN=1      # Включить ECDSA подписание
make USE_LWIP=1             # Включить Ethernet + LwIP (по умолчанию)
make USE_WEBUSB=1           # Включить USB WebUSB интерфейс
make USE_TEST_SEED=1        # Использовать зашитый test seed (только разработка)
make USE_RNG_DUMP=1         # Включить тестирование RNG
make SKIP_OLED=1            # Пропустить I2C/OLED если дисплей зависает

# Комбинированный пример:
make USE_CRYPTO_SIGN=1 USE_TEST_SEED=1 USE_RNG_DUMP=1
```

### Типичная сборка и прошивка

```bash
# Сборка
cd /data/projects/CryptoWallet
make clean
make all USE_CRYPTO_SIGN=1

# Прошивка
make flash

# Мониторить вывод UART
python3 -m serial.tools.miniterm /dev/ttyACM0 115200 --raw
```

---

## 📊 Метрики Производительности

| Операция | Время | Примечания |
|----------|-------|-----------|
| **ECDSA подпись** | 50-200ms | Зависит от RNG |
| **SHA-256** | 5-10ms | Аппаратный ускоритель |
| **Переключение задачи** | 5-10µs | Задержка переключения контекста |
| **SysTick прерывание** | 3-5µs | За тик (1ms) |
| **Обновление OLED** | 50ms | I2C @ 100кГц |
| **HTTP запрос** | 100-300ms | Включая ожидание подписи |
| **Время загрузки** | 100-500ms | Включая инициализацию FreeRTOS |

---

## 💾 Стек System Software

**Слоистая архитектура:**
```
┌─────────────────────────────────────┐
│  Слой приложения (task_*.c)         │ ← Код задач
│  - Подписание, Сеть, Дисплей, Ввод │
├─────────────────────────────────────┤
│  Слой Middleware                    │
│  - LwIP (сеть)                      │
│  - trezor-crypto (криптография)     │
│  - FreeRTOS (ядро)                  │
├─────────────────────────────────────┤
│  Слой HAL (hal/)                    │ ← Драйверы
│  - UART, GPIO, I2C, SPI, USB, Eth   │
├─────────────────────────────────────┤
│  Слой CMSIS (stm32h7xx_hal_*.h)     │ ← Определения регистров
├─────────────────────────────────────┤
│  ARM Cortex-M7 Core                 │ ← CPU
│  - Кэш, MPU, FPU, и т.д             │
└─────────────────────────────────────┘
```

**Зависимости модулей:**
```
task_sign.c
├─ FreeRTOS (queue, mutex, event)
├─ crypto_wallet.c
│  └─ trezor-crypto
│     └─ secp256k1, BIP-39/32
└─ memzero.c (безопасное очищение буфера)

task_net.c
├─ FreeRTOS (queue)
├─ LwIP
│   ├─ TCP/IP стек
│   ├─ DHCP клиент
│   └─ HTTP сервер
└─ eth_phy_driver.c

task_display.c
├─ FreeRTOS (queue)
└─ i2c_driver.c → SSD1306 OLED

task_user.c
├─ FreeRTOS (event, queue)
└─ gpio_driver.c → GPIO_PC13 (кнопка)
```

---

## 🔒 Соображения безопасности (системный уровень)

**Безопасность памяти:**
- **Защита от переполнения стека**: Фиксированные размеры стеков предотвращают неконтролируемый рост
- **Фрагментация кучи**: Отдельная куча снижает предсказуемость состояния кучи
- **Чувствительные данные**: Очищаются с помощью volatile писаний (предотвращает оптимизацию компилятора)

**Безопасность реального времени:**
- **Timing атаки**: Криптографические операции варьируются с RNG (timing непредсказуем)
- **Power анализ**: Сложный (требуется осциллограф, непрактично для анализа кода)
- **Fault injection**: Требует физического доступа (оборудование для глитчинга)

**Безопасность выполнения:**
- **Без рандомизации адресов** (ограничение embedded)
- **Без DEP/NX бита** (ограничение Cortex-M7)
- **Проверка загрузчика** (если включено): Гарантирует только авторизованный код

**Безопасность IPC:**
- **Переполнение очереди**: Статически выделяется (без ошибки выделения)
- **Deadlock mutex**: FreeRTOS наследование приоритета предотвращает
- **Изоляция задач**: Нет (все задачи делят адресное пространство, по дизайну)

---

## 📚 Справочник модулей

### Основная безопасность и подписание

| Модуль | Назначение | Расположение |
|--------|-----------|--------------|
| **main.c** | Точка входа FreeRTOS, инициализация IPC, создание задач | `Core/Src/main.c` |
| **task_sign.c** | FSM конвейера подписания, валидация транзакции, ECDSA | `Core/Src/task_sign.c` |
| **crypto_wallet.c** | Обёртка trezor-crypto, производная ключа, RNG | `Core/Src/crypto_wallet.c` |
| **tx_request_validate.c** | Ворота валидации адреса/суммы/валюты | `Core/Src/tx_request_validate.c` |
| **memzero.c** | Безопасное очищение буфера через volatile писания | `Core/Src/memzero.c` |
| **sha256_minimal.c** | Fallback SHA-256 (USE_CRYPTO_SIGN=0) | `Core/Src/sha256_minimal.c` |
| **wallet_seed.c** | Управление seed (test/разработка) | `Core/Src/wallet_seed.c` |
| **task_security.c** | Легаси FSM с mock крипто (аудит/тест) | `Core/Src/task_security.c` |

### Сеть и коммуникация

| Модуль | Назначение | Расположение |
|--------|-----------|--------------|
| **task_net.c** | LwIP/Ethernet, HTTP сервер порт 80 | `Src/task_net.c` |
| **usb_webusb.c** | WebUSB vendor интерфейс, бинарный протокол | `Core/Src/usb_webusb.c` |
| **app_ethernet_cw.c** | Ethernet link FSM, DHCP состояние | `Src/app_ethernet_cw.c` |
| **time_service.c** | SNTP синхронизация, UTC строки | `Core/Src/time_service.c` |
| **usb_device.c** | Инициализация USB устройства (HSI48) | `Core/Src/usb_device.c` |
| **usbd_conf_cw.c** | USB BSP конфигурация, статический алокатор | `Core/Src/usbd_conf_cw.c` |
| **usbd_desc_cw.c** | USB дескрипторы, WebUSB Platform UUID | `Core/Src/usbd_desc_cw.c` |

### Пользовательский интерфейс и I/O

| Модуль | Назначение | Расположение |
|--------|-----------|--------------|
| **task_display.c** | SSD1306 UI (полный), 4-строчный рендер | `Core/Src/task_display.c` |
| **task_display_minimal.c** | Минимальный UI для minimal-lwip | `Core/Src/task_display_minimal.c` |
| **task_user.c** | Debounce кнопки (PC13), логика подтверждения/отклонения | `Core/Src/task_user.c` |
| **task_io.c** | LED индикаторы (alive, сеть, alert) | `Core/Src/task_io.c` |

### Железо и система

| Модуль | Назначение | Расположение |
|--------|-----------|--------------|
| **hw_init.c** | Тактирование, MPU/кэш, GPIO, I2C1, UART, USB, RNG | `Core/Src/hw_init.c` |
| **stm32h7xx_hal_msp.c** | MSP callbacks (I2C1, UART) | `Core/Src/stm32h7xx_hal_msp.c` |
| **stm32h7xx_it.c** | Обработчики прерываний (SysTick, ETH) | `Core/Src/stm32h7xx_it.c` |
| **stm32h7xx_it_systick.c** | Обработчик SysTick (minimal-lwip) | `Core/Src/stm32h7xx_it_systick.c` |
| **stm32h7xx_it_usb.c** | Обработчик прерывания USB OTG HS | `Core/Src/stm32h7xx_it_usb.c` |
| **ssd1306_conf.h** | Конфигурация драйвера дисплея (I2C1, 128×32) | `Drivers/ssd1306/ssd1306_conf.h` |

### Общие контракты

| Заголовок | Назначение | Расположение |
|-----------|-----------|--------------|
| **wallet_shared.h** | Типы IPC, очереди, события, контекст дисплея | `Core/Inc/wallet_shared.h` |
| **lwipopts.h** | Compile-time конфигурация LwIP (куча, TCP, SNTP) | `Core/Inc/lwipopts.h` |
| **main.h** | Пины платы, определения LED, макро UART | `Core/Inc/main.h` |

### Детальная документация

Каждый модуль имеет комплексную документацию в `docs_src/`:
- **Abstract** - Обзор бизнес-логики
- **Logic Flow** - Пошаговый алгоритм
- **Dependencies** - Взаимодействие с другими модулями
- **Relations** - Контекст системы

Пример: [`docs_src/task_sign_ru.md`](docs_src/task_sign_ru.md) детально объясняет 9-состояние FSM подписания.

---

## 🔗 Связанная Документация

**Комплексный анализ и сравнение:**
- [`docs_src/analysis/PROJECTS_COMPARISON_AND_UPDATES_ru.md`](docs_src/analysis/PROJECTS_COMPARISON_AND_UPDATES_ru.md) - Полное сравнение stm32_secure_boot vs CryptoWallet
- [`docs_src/analysis/ARCHITECTURE_DETAILED_ru.md`](docs_src/analysis/ARCHITECTURE_DETAILED_ru.md) - Полная архитектура системы
- [`docs_src/analysis/QUICK_REFERENCE_ru.md`](docs_src/analysis/QUICK_REFERENCE_ru.md) - Команды и устранение неполадок

**Документация модулей:**
- [`docs_src/README.md`](docs_src/README.md) - Полный индекс всех модулей с иерархической навигацией

---

## ✅ Статус

- ✅ **Production-ready** прошивка
- ✅ **Полная криптографическая** поддержка подписания
- ✅ **Real-time** планирование задач (FreeRTOS)
- ✅ **Многопротокольная** связь (HTTP/WebUSB/UART)
- ✅ **Безопасное** управление ключами (BIP-39/32)
- ✅ **Профессиональная** документация

---

## 📖 Дополнительное чтение

Для более глубокого понимания и исследования:

1. **Быстрый старт:** [`docs_src/analysis/QUICK_REFERENCE_ru.md`](docs_src/analysis/QUICK_REFERENCE_ru.md)
2. **Архитектура:** [`docs_src/analysis/ARCHITECTURE_DETAILED_ru.md`](docs_src/analysis/ARCHITECTURE_DETAILED_ru.md)
3. **Сравнение:** [`docs_src/analysis/PROJECTS_COMPARISON_AND_UPDATES_ru.md`](docs_src/analysis/PROJECTS_COMPARISON_AND_UPDATES_ru.md)
4. **Индекс модулей:** [`docs_src/README.md`](docs_src/README.md)

**Выберите ваш язык:**
- 🇬🇧 [English](README.md)
- 🇷🇺 Русский (выше)
- 🇵🇱 [Polski](README_pl.md)

---

**CryptoWallet** - Демонстрация безопасного, real-time дизайна встроенных систем на STM32H743.

*Последнее обновление: 2026-03-20*
